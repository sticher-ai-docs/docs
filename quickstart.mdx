---
title: "Quickstart"
description: "Get access to the web app and the authoring environment — the one-time setup that unlocks StitcherAI end to end."
---

Before you can do anything else in StitcherAI, you need access to two surfaces: the **web app**, where you manage connections, runs, and schedules, and the **authoring environment**, where you build the cost model itself. Both use the same identity, but each has its own one-time setup.

<Frame caption="The authoring environment's Welcome to StitcherAI start page — Start a Chat, Quick Start Guide, and Manage Pipeline, with the four stages below.">
  <img src="/images/quickstart-authoring-environment.png" alt="The authoring environment's Welcome to StitcherAI start page" />
</Frame>

<Frame caption="The web app's Dashboard — pipeline run history, success/failure counts, and recent runs.">
  <img src="/images/quickstart-webapp-dashboard.png" alt="The web app's Dashboard" />
</Frame>

## Prerequisites

- Access to the relevant [environment](/access-and-administration) — provisioned by StitcherAI and based on your organization's identity provider
- GitHub access to your pipeline configuration repo — see [Access & administration](/access-and-administration)

## Access the web app

The web app at **[app.stitcher.ai](https://app.stitcher.ai)** is where you manage environments, data connections, runs, schedules, tags, and assistant sessions.

<Steps>
  <Step title="Sign in">
    StitcherAI provisions your account — your email and password. Organizations can optionally connect their own identity provider via SAML for single sign-on with a corporate identity. Either way, the same identity is used for the web app and the VS Code editor.

    <Frame caption="Signing in to the web app with a work email.">
      <img src="/images/quickstart-webapp-login.png" alt="Signing in to the web app with a work email" />
    </Frame>
  </Step>
  <Step title="Confirm your environment">
    Once signed in, you land in the [environment](/access-and-administration) your role grants you access to. If you work across more than one environment, you can switch between the ones you have access to from within the app.
  </Step>
</Steps>

<Note>
  This is the **same identity** you'll use to log in to the authoring environment below.
</Note>

## Set up the authoring environment

Before you can build your cost model in the editor, set up the authoring environment: install the extension, open your pipeline configuration repo, and log in.

### Set up the environment

<Steps>
  <Step title="Install VS Code">
    Download and install [VS Code](https://code.visualstudio.com) for your operating system, then open it.

    <Frame caption="The Extensions icon in VS Code's Activity Bar.">
      <img src="/images/quickstart-vscode-extensions-icon.png" alt="The Extensions icon in VS Code's Activity Bar" />
    </Frame>
  </Step>
  <Step title="Install the StitcherAI extension">
    In VS Code, open the **Extensions** view — click the Extensions icon in the Activity Bar on the left (or press **Ctrl+Shift+X** on Windows/Linux, **Cmd+Shift+X** on Mac). Search for **StitcherAI** and click **Install** on the [StitcherAI extension](https://marketplace.visualstudio.com/items?itemName=StitcherAIInc.stitcherai-editor).

    <Frame caption="Searching for and installing the StitcherAI extension from the Extensions view.">
      <img src="/images/quickstart-stitcherai-extension-search.png" alt="Searching for and installing the StitcherAI extension" />
    </Frame>
  </Step>
  <Step title="Clone your pipeline configuration repo">
    Find the repository URL in the web app: **Pipelines → your pipeline → Visit repository**.

    <Frame caption="Finding a pipeline's repository URL in the web app: Pipelines → your pipeline → Visit repository.">
      <img src="/images/quickstart-visit-repository.png" alt="Finding a pipeline's repository URL in the web app" />
    </Frame>

    Then use VS Code's built-in Git support: open the Command Palette (**Ctrl+Shift+P** on Windows/Linux, **Cmd+Shift+P** on Mac), run **Git: Clone**, paste the repo URL when prompted, then choose a folder on your machine to clone it into.

    If you're comfortable with the command line, this does the same thing:

    ```bash
    git clone <your-pipeline-repo-url>
    ```
  </Step>
  <Step title="Open that repo in VS Code">
    If you used **Git: Clone** above, VS Code offers to open the cloned folder right away — accept that. Otherwise, use **File → Open Folder...** (Windows/Linux) or **File → Open...** (Mac) and select the folder you cloned.
  </Step>
  <Step title="Log into the StitcherAI extension">
    With the repo open, click **log in** in the StitcherAI extension. A new browser tab opens — sign in with the same identity you use for the web app. Confirming there completes the login and hands control back to VS Code.

    <Frame caption="Logging in to the StitcherAI extension from the start page.">
      <img src="/images/quickstart-extension-login.png" alt="Logging in to the StitcherAI extension from the start page" />
    </Frame>
  </Step>
</Steps>

<Note>
  Log in only **after** opening your pipeline configuration repo. The extension needs that project context to complete the login loop — logging in first, or with the wrong folder open, means the browser confirmation won't connect back to VS Code.
</Note>

### Verify it worked

Once logged in, the **Welcome to StitcherAI** start page appears, with **Start a Chat**, **Quick Start Guide**, and **Manage Pipeline** actions, and the four stages — Connect, Model, Build, Engage — laid out below.

Open the **Schema Browser** (the icon in the top bar, next to the AI chat icon) to confirm your data is connected. Right after logging in it shows a spinning icon while metadata is retrieved:

<Frame caption="The Schema Browser icon while metadata is being retrieved, right after logging in.">
  <img src="/images/quickstart-schema-browser-updating.png" alt="The Schema Browser icon while metadata is being retrieved" />
</Frame>

Once that finishes, it shows **Metadata refreshed at [time]**. From there you can browse by stage, by data source, or by column.

<Frame caption="The Schema Browser once metadata has refreshed, with browse-by-stage, -data-source, and -column options.">
  <img src="/images/quickstart-schema-browser.png" alt="The Schema Browser once metadata has refreshed" />
</Frame>

If you don't see these, confirm your pipeline configuration repo is still the open folder, then log out and back in.

## Next steps

<CardGroup cols={2}>
  <Card title="How to: Bring in a dataset" icon="database" href="/how-to/bring-in-a-dataset">
    Connect your first cost or business data source.
  </Card>
  <Card title="How a pipeline is composed" icon="diagram-project" href="/pipeline-overview">
    The four phases you'll build, and how configurations are authored — visually, by AI chat, or directly.
  </Card>
</CardGroup>
